# IMPORTANT: This is a template. Do not commit real secrets!
# Use sealed-secrets, external-secrets, or vault in production.
apiVersion: v1
kind: Secret
metadata:
  name: village-secrets
  namespace: agent-village
  labels:
    app.kubernetes.io/name: agent-village
type: Opaque
stringData:
  # Replace these with actual secrets or use external secret management
  LLM__ANTHROPIC_API_KEY: "your-anthropic-api-key"
  LLM__OPENAI_API_KEY: "your-openai-api-key"
  MEMORY__POSTGRES_URL: "postgresql+asyncpg://village:password@postgres-service:5432/village"
  MEMORY__REDIS_URL: "redis://redis-service:6379/0"
